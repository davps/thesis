

\section{Diferencia entre los \emph{LNType} y \emph{LN} del
\emph{SCL} y los nodos lógicos del dispositivo IED}

ESTO DE ACÁ EN REALIDAD NO VOY A COLOCAR

Existe una clara diferencia entre
los nodos lógicos definidos en 
el \textbf{DataTypeTemplates} del SCL, 
los nodos lógicos definidos en 
el elemento IED (\textbf{LN}) y 
en el elemento \textbf{Substation} 
del SCL y los nodos lógicos 
implementados en el IED.

Cada uno de estos nodos lógicos 
de acuerdo a su contexto, tienen
un rol bien definido. El
\textbf{LNType} es una plantilla
\emph{off--line} (sería lo más cercano a una clase) 
para generar objetos 



A class is a static off-line 
template to generate objects. 
A object is a runtime data 
or a group of datas 
according to the class. The objects are 
independend datas with 
its own behavior, and 
they are classified by the classes 
\todo{ESP:por las clases q la han generado}
which generated.
